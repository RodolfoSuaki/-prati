let livro = {
    nome: "A Guerra dos Tronos",
    genero: ["Romance, Fantasia, Ficção"],
    dataLancamento: 2000
}

let carro = {
    nome: "teste",
    genero: ["Romance, Fantasia, Ficção"],
    dataLancamento: 2000
}


for(let chave in livro){
    console.log(`${chave} : ${livro[chave]}`)
}

for(let chave of Object.keys(carro)){
    console.log(`${chave} : ${carro[chave]}`)
}